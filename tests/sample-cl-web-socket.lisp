(defpackage sample-cl-web-socket-test
  (:use :cl
        :sample-cl-web-socket
        :prove))
(in-package :sample-cl-web-socket-test)

;; NOTE: To run this test file, execute `(asdf:test-system :sample-cl-web-socket)' in your Lisp.

(plan nil)

;; blah blah blah.

(finalize)
