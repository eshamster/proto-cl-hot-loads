(defpackage proto-cl-hot-loads-test
  (:use :cl
        :proto-cl-hot-loads
        :prove))
(in-package :proto-cl-hot-loads-test)

;; NOTE: To run this test file, execute `(asdf:test-system :proto-cl-hot-loads)' in your Lisp.

(plan nil)

;; blah blah blah.

(finalize)
